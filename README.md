# gmmm-python-server

## Setup (uv)

Install `uv` (macOS):

```bash
brew install uv
```

Create/update the local virtualenv and install deps:

```bash
uv sync
```

Run a script:

```bash
uv run python src/signals_fetcher.py
```
